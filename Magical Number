def findMagicalNumber(self, N : int, arr : List[int]) -> int:
        for i in range(len(arr)):
            if arr[i]==i:
                return i
                break
        return -1
            
